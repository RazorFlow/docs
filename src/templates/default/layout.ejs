<!doctype html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.min.css">
    <script type="text/javascript" src="/static/build/razorflow.min.js"></script>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-4">
                <ul class="nav">
                    <%- render('nav.ejs', {items: tree.findAllClassNames()}) %>
                </ul>
            </div>
            <div class="col-md-8">
                <% if(currentClass != 'index') { %>
                    <%- render('class.ejs', {tree: tree, item: tree.findNodeByClassName(currentClass)}) %>
                <% } else { %>
                    <%- render('index.ejs', {}) %>
                <% } %>
            </div>
    </div>
</body>
</html>